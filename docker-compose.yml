version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Database configuration
      - POSTGRES_CONNECTION_MODE=direct
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=aitrace
      # WARNING: This is an insecure default key for local development only!
      # For production, use docker-compose.prod.yml with a secure SECRET_KEY
      - SECRET_KEY=insecure-local-development-key-change-in-production-min-32-chars
      - ENV=dev
      - LOG_LEVEL=INFO
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: aitrace
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
